# Agent Instructions Moved

This file remains only as a compatibility entry point for tools that still look in the legacy location.

Project agent and LLM guidance is stored in `.agents/`. Start with `.agents/CLAUDE.md` and keep new guardrails, directives, memories, and agent notes under `.agents/`.
